{
  "name": "a-chang-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.mjs && node scripts/check.mjs",
    "dev": "npm run build && python3 -m http.server 4173 -d dist",
    "check": "node scripts/check.mjs"
  }
}
